const Math = {
    Add: function (x, y) {
        return x + y;
    },
    Multiply: function(x, y) {
        return x * y;
    },
    Subtract: function (x, y) {
        return x - y;
    },
    Divide: function(x, y) {
        return x / y;
    }
}

module.exports = Math